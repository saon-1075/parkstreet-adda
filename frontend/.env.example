# Copy to `.env` and fill in from your Supabase project settings (API section).
# These are the ONLY secrets — everything brand-specific lives in
# src/config/restaurant.config.ts instead.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
